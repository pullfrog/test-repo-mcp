{
  "name": "robin-mcp",
  "version": "1.0.0",
  "description": "A sample MCP server with a story tool",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=build/index.js --minify",
    "prepare": "npm run build",
    "start": "node build/index.js",
    "test": "node test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "esbuild": "^0.27.2"
  }
}
